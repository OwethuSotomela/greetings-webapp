language: node_js
node_js:
  - 12
before_script:
  - psql -c 'create database travis_ci_test;' -U postgres
  - psql -u postgres -d travis_ci_test -a -f table.sql
services:
  - postgresql
addons:
  postgresql: '9.4'
  apt:
    packages:
      - postgresql-server-dev-9.4